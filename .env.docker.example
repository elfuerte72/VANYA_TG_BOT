# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHANNEL_ID=@your_channel_username_or_chat_id

# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4-1106-preview

# Database Configuration (for Docker)
DB_PATH=/app/data/users.db
DATABASE_SECRET_KEY=your_very_secure_secret_key_for_encryption_32chars_minimum

# Logging Configuration
LOG_LEVEL=INFO

# System Prompt for OpenAI (используется в коде)
SYSTEM_PROMPT=f"""
Ты — помощник по питанию. Вот рассчитанное КБЖУ:
- Калории: {kcal}
- Белки: {protein} г
- Жиры: {fat} г
- Углеводы: {carbs} г

Разбей на {"3" if kcal < 2000 else "4"} приёма пищи. - расписать продукты в сухом виде.  цифры и названия приёмов пищи. Дай ответ в виде массива JSON, где каждый элемент содержит:
- meal (название приёма пищи)
- product (Название продуктов)
- calories
- protein
- fat
- carbs
"""

# Docker-specific settings
# Установить в 'production' для продакшена
ENVIRONMENT=development